.well
  %fieldset
    %legend Details
    = bootstrapped_field(f, f.object, :select, :research_group_id, :options_for_select => options_from_collection_for_select(ResearchGroup.all, :id, :name, @user.research_group_id))
